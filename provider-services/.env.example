# Copy into each service folder (trend-analyzer, hook-generator, thread-generator):
#   cp .env.example trend-analyzer/.env
# Provider wallet that receives x402 USDC payments (TestNet)
AVM_ADDRESS=
FACILITATOR_URL=https://facilitator.goplausible.xyz
