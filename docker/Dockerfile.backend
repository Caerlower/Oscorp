FROM python:3.12-slim
WORKDIR /app
COPY backend /app/backend
RUN pip install --no-cache-dir /app/backend
WORKDIR /app/backend
ENV OSCORP_DATA_DIR=/tmp/oscorp-data
CMD uvicorn app.api.main:app --host 0.0.0.0 --port ${PORT:-8000}
