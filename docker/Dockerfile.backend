FROM python:3.12-slim
WORKDIR /app
COPY shared /app/shared
COPY backend /app/backend
RUN pip install --no-cache-dir -e /app/shared -e /app/backend
WORKDIR /app/backend
CMD ["uvicorn", "app.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
